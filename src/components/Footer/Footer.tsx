// node_modules

// components

// styles
import styles from './Footer.module.scss';


const Footer = () => {
    return (
        <h1 className={styles.footer}>This is footer</h1>
    )
}

export { Footer }