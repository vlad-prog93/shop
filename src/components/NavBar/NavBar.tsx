// node_modules

// components

// styles
import styles from './NavBar.module.scss';

const NavBar = () => {
    return (
        <nav>
            <h1>This is NavBar</h1>
        </nav>
    )
}

export { NavBar }